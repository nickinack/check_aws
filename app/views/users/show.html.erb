<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
       <div class="user">
       <div class = "img-responsive" id="profile-pic"> <%= gravatar_for @user %> </div>
        <%= @user.name %>
        </div>
      </h1>
    </section>
  </aside>
</div>
